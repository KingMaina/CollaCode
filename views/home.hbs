<!-- Header -->
<header class="ex-header">
    <div class="container">
        <div class="row">
            <div class="col-xl-10 offset-xl-1">
                <h1>Your Workspace</h1>
            </div>
        </div>
    </div>
</header>


<!-- Basic -->
<div class="ex-basic-1 pt-5 pb-5">
    <!-- Modal -->
    <div class="modal fade p-5" id="createTask" tabindex="-1" role="dialog" aria-labelledby="modalCreateTask"
        aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modalTitle">Create a task</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form method="post">
                        <div class="form-row">
                            <div class="form-group col-md-6">
                                <label for="taskTitle">Tile</label>
                                <input name="title" type="text" class="form-control" id="taskTitle"
                                    placeholder="e.g. main" required>
                            </div>
                            <div class="form-group col-md-6">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="taskDescription">Description</label>
                            <textarea name="description" cols="5" rows="3" class="form-control" id="taskDescription"
                                placeholder="[optional] e.g. Code for calculator"></textarea>
                        </div>
                        <div class="form-row">
                            <div class="form-group col-md-4">
                                <label for="taskLanguages">Language</label>
                                <select name="language" id="taskLanguages" class="form-control" required>
                                    <option selected>Choose...</option>
                                    <option value="javascript">Javascript</option>
                                    <option value="python">Python</option>
                                    <option value="html">HTML</option>
                                    <option value="css">CSS</option>
                                </select>
                            </div>
                        </div>
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                        <button class="btn btn-primary" id="taskSubmit">Create</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    {{!-- <div class="modal fade p-5" id="deleteTaskModal" tabindex="-1" role="dialog" aria-labelledby="modalDeleteTask"
        aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteTaskTitle">Delete task</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to delete this task?</p>
                    <p class="text-danger">This action cannot be undone!</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                    <button id="modalDelete" type="button" class="btn btn-danger">Delete</button>
                    <input type="hidden" value="" id="taskID"/>
                </div>
            </div>
        </div>
    </div> --}}
    <div class="container">
        <div class="row">
            <div class="col-xl-10 offset-xl-1">
                <ul class="row mb-5 list-unstyled">
                    <li class="col-sm-12 mb-4">
                        <div class="card text-center">
                            <div class="card-body">
                                <button type="button" class="btn btn-primary" data-toggle="modal"
                                    data-target="#createTask">
                                    Create a task
                                </button>
                            </div>
                        </div>
                    </li>
                    {{#if taskList}}
                    {{#each taskList}}
                    <li class="col-sm-6 mb-4">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">{{this.title}}</h5>
                                <p class="card-text">{{this.description}}</p>
                                <p class="card-text">{{this.language}}</p>
                                <a href="/task/{{this.id}}" class="btn btn-info">Edit</a>
                                <button type="button" class="btn btn-danger" data-toggle="modal"
                                    data-target="#deleteTaskModal" onclick="deleteTask({{this.id}})">Delete</button>
                                <input type="hidden" value="{{this.id}}" />
                            </div>
                        </div>
                    </li>
                    {{/each}}
                    {{else}}
                    <li class="col-sm-12 mb-4 text-center">
                        <div class="card">
                            <div class="card-body">
                                <p class="card-text">You have no tasks created. Create one!</p>
                            </div>
                        </div>
                    </li>
                    {{/if}}
                </ul>
            </div> <!-- end of col -->
        </div> <!-- end of row -->
    </div> <!-- end of container -->
</div>